{
  "name": "push2cloud-example",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "concurrently --kill-others \"npm run startApi\" \"npm run startHost\"",
    "startApi": "cd api && npm start",
    "startHost": "cd host && npm start"
  },
  "engines": {
    "node": "5.x",
    "npm": "3.x"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^2.0.0"
  }
}
